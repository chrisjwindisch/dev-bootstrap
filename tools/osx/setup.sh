#!/bin/bash

# utilities

# ---------------------------------------------------------------

# setup tools needed to run a project on a new laptop
brew install gh # install github tools
brew install node
npm install --global yarn
brew install --cask google-cloud-sdk # install google-cloud sdk
brew install firebase-cli
brew install md5sha1sum
